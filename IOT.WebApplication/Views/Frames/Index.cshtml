@model IEnumerable<IOT.Domain.Frames.Frame>

@{
    ViewBag.Title = "Index";
}


@*<p>
    @Html.ActionLink("Create New", "Create")
</p>*@
<table class="table">
    <tr>
        <th></th>
    </tr>

    <tr>
        <td>
            @Html.DisplayNameFor(frame => frame.Id)
        </td>
        <td>
            @Html.DisplayNameFor(frame => frame.Name)
        </td>
        <td>
        </td>
    </tr>
@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.ActionLink("Details", "Details", new {  id=item.Id }) 
        </td>
    </tr>
}

</table>
